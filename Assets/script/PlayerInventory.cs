using UnityEngine;

public static class PlayerInventory
{
    // Indique si le joueur possède le diamant
    public static bool HasDiamond { get; set; } = false;

    // Tu peux ajouter d'autres objets dans le futur
    // public static bool HasKey { get; set; } = false;
}
